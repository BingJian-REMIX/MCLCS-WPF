## 本次快照：T2 第二梯队完成

这是四个梯队中**第二梯队**完成后的存档点：规格符合度整改（全局 ToggleSwitch、工具箱侧边栏、设置页补开关），不含侧边栏钉住（T2-3 延后）。

| 项目 | 内容 |
|---|---|
| 快照标记 | `T2` |
| 程序集版本 | `MCLCS.App` = 2.1.0 |
| 梯队范围 | 第二梯队：规格符合度整改 |
| Core 编译 | **0 错误** |
| 单元测试 | **57 项全部通过** |
| 工具箱面板数 | 17（侧边栏导航模式） |

### T1 → T2 变更对比

#### Core 层改动（1 处）

| 文件 | 改动 | 说明 |
|---|---|---|
| `Core/Profiles/LauncherProfile.cs` | +10 行 | 新增 `LaunchCompatCheckEnabled`（启动前兼容性检测）、`ServerPackCacheEnabled`（资源包缓存总开关）两个字段 |

#### App ViewModels 层（1 新增 + 1 修改）

| 文件 | 改动 | 说明 |
|---|---|---|
| `App/ViewModels/ToolboxViewModel.cs` | **新建 97 行** | 工具箱侧边栏 VM：17 个 `ToolboxPanelItem` 注册表（图标+标题+工厂），`SelectedPanel` / `SelectedView` 联动，懒加载视图 |
| `App/ViewModels/SettingsViewModel.cs` | +72 行 | 新增 6 个 bool 属性 + 字段：FileWatchEnabled、PrewarmEnabled、HudEnabled、LaunchCompatCheckEnabled、AutoRepairResourcePacks、ServerPackCacheEnabled；Load/Save 写入对应 profile 字段 |

#### App Views 层（1 重写 + 8 修改）

| 文件 | 改动 | 说明 |
|---|---|---|
| `App/Views/ToolboxView.xaml` | **重写** | TabControl → 左侧侧边栏（48px 图标列）+ 右侧 ContentControl，17 个面板全部进入侧边栏 |
| `App/Views/ToolboxView.xaml.cs` | 不变 | 精简，DataContext 在 XAML 中设置 |
| `App/Views/SettingsView.xaml` | +44 行 | 新增 6 个 ToggleSwitch 绑定（通用/启动/下载）；原有 5 个 CheckBox → ToggleSwitch + 3 AI 子开关 + 玩法偏好 DataTemplate + 自动更新 |
| `App/Views/SettingsView.xaml.cs` | 不变 | — |
| `App/Views/LogView.xaml` | +4 行 | "仅错误" CheckBox → ToggleSwitch + TextBlock |
| `App/Views/ModpackView.xaml` | +25 行 | 5 个"包含/不包含"CheckBox → ToggleSwitch + TextBlock |
| `App/Views/RedundantCleanView.xaml` | +3 行 | "直接删除" CheckBox → ToggleSwitch + TextBlock |
| `App/Views/DownloadPageView.xaml` | +3 行 | "隔离安装" CheckBox → ToggleSwitch + TextBlock |

#### 保留不动的 CheckBox（合计 2 处）

| 文件 | 说明 |
|---|---|
| `ScreenshotView.xaml` DataTemplate | 多选删除列——这是列表选择框，不是开关 |
| `RedundantCleanView.xaml` DataTemplate | 多选清理列——同上 |

### 功能完成度

| # | 功能 | 状态 | 说明 |
|---|---|---|---|
| T2-1 | 全局 ToggleSwitch | ✅ | 28 处 CheckBox → 22 处已替换，6 处保留（列表多选场景） |
| T2-2 | 工具箱侧边栏导航 | ✅ | 左侧 48px 图标列 + 右侧 ContentControl 懒加载，选中高亮 |
| T2-3 | 侧边栏点击钉住 | ⏸️ 延后 | 用户明确要求跳过 |
| T2-4 | 设置页补 6 个开关 | ✅ | 通用 1 + 启动 3 + 下载 2，LoadFromProfile / Save 全链路贯通 |

### 关键设计说明

- **侧边栏宽度 48px**（规格 1.4：收起 48-56px），仅图标。后续悬停展开方案预留了空间但当前未实现——该部分与主窗口侧边栏的悬停展开共用同一实现路径。
- **工具箱侧边栏选中态**：选中面板图标块高亮为主题色 + 白色前景；未选中为默认按钮背景 + 灰色前景。
- **PrewarmEnabled** 映射为 `PrewarmConfig.Mode != Off`（开→Light，关→Off），保存时写回对应枚举。
- **ServerPackCacheEnabled** 是独立 bool 字段，与 `ServerPackCacheMb`（容量上限）分离——关闭时进服不缓存，开启时用容量值。

### 各梯队剩余计划

1. ~~第一梯队 — Core 就绪，纯补 UI~~ ✅
2. ~~第二梯队 — 规格符合度整改~~ ✅（除侧边栏钉住延后）
3. **第三梯队** — 补齐半成品：推荐按钮绑定、服务器添加/右键、统计真实数据、性能曲线、存档四操作、AI 助手改版
4. **第四梯队** — 大工程新开发：皮肤编辑器像素引擎、开发工具三件套、HUD 叠加窗口、挂机工作流编辑器、真实连接服务器
