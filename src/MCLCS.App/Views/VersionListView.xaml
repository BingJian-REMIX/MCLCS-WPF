<UserControl x:Class="MCLCS.App.Views.VersionListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:MCLCS.App.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="520" d:DesignWidth="920"
             d:DataContext="{d:DesignInstance vm:VersionListViewModel}">
    <UserControl.DataContext>
        <vm:VersionListViewModel/>
    </UserControl.DataContext>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="已安装版本" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="刷新" Command="{Binding RefreshCommand}" Margin="12,0,0,0" Padding="8,2"/>
        </StackPanel>

        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Versions}"
                 SelectedItem="{Binding SelectedVersion}"
                 DisplayMemberPath="DisplayName"
                 BorderThickness="1"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="启动游戏" Command="{Binding LaunchCommand}" Width="120" Padding="0,4"/>
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="12,0,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>
