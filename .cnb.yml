# 更新源已迁移至 GitHub：
#   updates/latest.json 直接放在公开 GitHub 仓库内，通过 jsDelivr CDN 国内直连，
#   地址形如 https://cdn.jsdelivr.net/gh/<owner>/<repo>@main/updates/latest.json
# 本仓库（cnb.cool）不再需要任何 CI 部署脚本，故置空以免误触发默认流水线。
main:
  push: []
