<UserControl x:Class="MCLCS.App.Views.DownloadCenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MCLCS.App.ViewModels">
    <UserControl.DataContext>
        <vm:DownloadCenterViewModel/>
    </UserControl.DataContext>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="160"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="下载中心 (Modrinth)" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBox Width="240" Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}" ToolTip="搜索关键词"/>
            <Button Content="搜索" Command="{Binding SearchCommand}" Margin="8,0,0,0" Padding="8,2"/>
            <ComboBox Width="120" Margin="8,0,0,0" ItemsSource="{Binding Loaders}" SelectedItem="{Binding SelectedLoader}" ToolTip="加载器"/>
            <ComboBox Width="120" Margin="8,0,0,0" ItemsSource="{Binding GameVersions}" SelectedItem="{Binding SelectedGameVersion}" ToolTip="游戏版本"/>
            <ComboBox Width="140" Margin="8,0,0,0" ItemsSource="{Binding ProjectTypes}" SelectedItem="{Binding SelectedProjectType}" ToolTip="类型"/>
        </StackPanel>

        <ListBox Grid.Row="2" ItemsSource="{Binding Results}" SelectedItem="{Binding SelectedResult}"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 Background="{DynamicResource InputBackground}" Foreground="{DynamicResource InputForeground}" BorderBrush="{DynamicResource InputBorder}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="4">
                        <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Summary}" TextWrapping="Wrap" Opacity="0.7"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="加入队列" Command="{Binding EnqueueCommand}" Width="100" Padding="0,4"/>
            <Button Content="开始下载队列" Command="{Binding StartQueueCommand}" Width="120" Margin="8,0,0,0" Padding="0,4"
                    Background="{DynamicResource AccentBrush}" Foreground="White" BorderThickness="0"/>
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="12,0,0,0"/>
            <Button Content="清空已完成" Command="{Binding ClearCompletedCommand}" Width="100" Margin="12,0,0,0" Padding="0,4"/>
        </StackPanel>

        <ListBox Grid.Row="4" ItemsSource="{Binding Queue}" Margin="0,8,0,0"
                 Background="{DynamicResource InputBackground}" Foreground="{DynamicResource InputForeground}" BorderBrush="{DynamicResource InputBorder}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="60"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock FontWeight="Bold" Text="{Binding Title}"/>
                            <Grid Height="22" Margin="0,2,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <!-- bug.txt #7：细环形进度条（对齐 Linux 的 Ellipse+Arc 细环） -->
                                <Grid Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0">
                                    <Ellipse Width="18" Height="18" Stroke="{DynamicResource ProgressBackground}"
                                             StrokeThickness="2.5"/>
                                    <Ellipse Width="18" Height="18" Stroke="{DynamicResource AccentBrush}"
                                             StrokeThickness="2.5" StrokeEndLineCap="Round"
                                             StrokeDashArray="{Binding Progress, Converter={StaticResource ProgressToRingDash}, ConverterParameter=56.55}">
                                        <Ellipse.RenderTransform>
                                            <RotateTransform Angle="-90" CenterX="9" CenterY="9"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                    <TextBlock Text="{Binding Progress, StringFormat={}{0:0}%}" FontSize="8"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                               Foreground="{DynamicResource AccentBrush}"/>
                                </Grid>
                                <TextBlock Grid.Column="1" Text="{Binding Status}" VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis" Opacity="0.8"/>
                            </Grid>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="{Binding Status}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                        <Button Grid.Column="2" Content="暂停" Command="{Binding DataContext.PauseItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}" Padding="2" Margin="4,0"/>
                        <Button Grid.Column="3" Content="取消" Command="{Binding DataContext.CancelItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}" Padding="2"/>
                        <Button Grid.Column="4" Content="移除" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}" Padding="2"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
