## 本次快照：T1 第一梯队完成

这是四个梯队中**第一梯队**完成后的存档点。6 个功能一次性补完，工具箱从 11 个面板扩展到 16 个。

| 项目 | 内容 |
|---|---|
| 快照标记 | `T1` |
| 程序集版本 | `MCLCS.App` = 2.1.0 |
| 梯队范围 | 第一梯队：Core 就绪、纯补 UI |
| Core 编译 | **0 错误**（仅 8 个 CA1416 预存警告，非新增代码） |
| 单元测试 | **57 项全部通过** |
| 工具箱面板数 | 11 → 16（新增 5 面板 + 1 已调整为独立窗口入口） |

### T0 → T1 变更对比

#### 新增文件（16 个）

**Core 层改动（5 处）：**

| 文件 | 改动 | 说明 |
|---|---|---|
| `Core/Profiles/LauncherProfile.cs` | +7 行 | 新增 `FileWatchEnabled` 字段（规格 2.4 通用设置） |
| `Core/Toolbox/FileChangeDetector.cs` | +15 行 | 新增 `PreviewChanges()` 方法：只看不更新快照，供工具箱面板反复展示而不动基线 |
| `Core/Resources/ServerResourcePackCache.cs` | +36 行 | 新增 `Export()` / `ExportToResourcePacks()` / `Remove()` / `FilePathOf()` / `SuggestExportName()` 方法，支撑可视化面板 |
| `Core/Toolbox/DataPackConflictDetector.cs` | +3 行 | `Scan()` 体内接入内置规则库（`FindConflicts` 调用） |
| `Core/Statistics/AnnualReport.cs` | +64 行 | 新增 `ExportToken()` / `ImportToken()` 分享功能（MCAR1. 前缀，JSON → Deflate → Base64Url） |

**App ViewModels 层（6 个新文件）：**

| 文件 | 行数 | 面板 |
|---|---|---|
| `App/ViewModels/BackupViewModel.cs` | 408 | 备份管理器 — 策略、手动备份、恢复、清理、浏览 |
| `App/ViewModels/NbtViewModel.cs` | 454 | NBT 编辑器 — 树状编辑、编辑/新增/删除/重命名、导出文本 |
| `App/ViewModels/DataPackViewModel.cs` | 303 | 数据包冲突检测 — 扫描、冲突列表、规则库更新 |
| `App/ViewModels/FileWatchViewModel.cs` | 134 | 文件变更检测 — 预览变更、重置基线、开关联动 Profile |
| `App/ViewModels/AnnualReportViewModel.cs` | 265 | 年度报告 — 按月/版本/时段聚合、Token 导出导入、本地解读 |
| `App/ViewModels/ServerPackViewModel.cs` | 236 | 服务器资源包缓存 — 列表、导出、清理、容量管理 |

**App Views 层（6 个新 UserControl + 6 个 code-behind）：**

| 文件（.xaml） | 行数 |
|---|---|
| `App/Views/BackupView.xaml` | 207 |
| `App/Views/NbtView.xaml` | 170 |
| `App/Views/DataPackView.xaml` | 239 |
| `App/Views/FileWatchView.xaml` | 117 |
| `App/Views/AnnualReportView.xaml` | 247 |
| `App/Views/ServerPackView.xaml` | 141 |

**App 服务 / 基础设施改动（5 处）：**

| 文件 | 改动 | 说明 |
|---|---|---|
| `App/Services/LaunchCoordinator.cs` | +55 行 | 新增 `CheckFileChangesAsync()` + `OpenFileWatchWindow()`，启动前检测新增文件并弹 Toast |
| `App/Services/ToastService.cs` | 新建 128 行 | 全局 Toast 通知服务：右下角堆叠、5 秒自动消失、「查看详情」回调、任意线程调用 |
| `App/Services/UIService.cs` | +14 行 | 新增 `SaveFile()` 方法（SaveFileDialog 封装） |
| `App/MainWindow.xaml` | +65 行 | 右下角 Toast 通知层（ItemsControl 绑定 ToastService.Items，含动画、色条、关闭/操作按钮） |
| `App/MainWindow.xaml.cs` | +13 行 | 新增 `Activated` 事件处理：焦点回归时触发文件变更检测（1 秒防抖） |

**更新文件（3 处）：**

| 文件 | 改动 | 说明 |
|---|---|---|
| `App/Views/ToolboxView.xaml` | +18 行 | 新增 6 个 TabItem：备份管理器、NBT 编辑器、数据包冲突检测、服务器资源包缓存、文件变更检测、年度报告 |
| `App/Views/GameView.xaml` | +12 行 | 统计区新增「年度报告」入口卡片（点击打开独立窗口） |
| `App/Views/GameView.xaml.cs` | +7 行 | 年度报告卡片点击事件 → `GameViewModel.OpenAnnualReportCommand` |
| `App/ViewModels/GameViewModel.cs` | +19 行 | 新增 `OpenAnnualReportCommand` + `OpenAnnualReport()` 窗口 |
| `App/App.xaml` | +2 行 | 新增全局 `NonEmptyToVis` 转换器 |

#### 功能完成度

| # | 功能 | 状态 | 说明 |
|---|---|---|---|
| 1 | 备份管理器 | ✅ | 策略卡片 + 手动备份/恢复/清理/浏览，Toast 反馈 |
| 2 | NBT 编辑器 | ✅ | TreeView 树状编辑 + 右侧编辑面板，快捷文件列表 |
| 3 | 数据包冲突检测 | ✅ | 存档选择 + 扫描 + 内置 + 可联网更新规则库，冲突严重度分级 |
| 4 | 文件变更检测 + Toast | ✅ | 启动前扫描 mods/resourcepacks/shaderpacks，Toast 非阻塞通知，焦点回归触发 |
| 5 | 年度报告 | ✅ | 统计卡片 + 月度柱状 + 版本排行 + Token 导出导入 + 游戏页入口 |
| 6 | 服务器资源包缓存 | ✅ | 列表展示 + 导出/清理 + 容量管理 + 空状态提示 |

### 工具箱面板变化

```
T0 (11 tab):  日志 → 存档 → 种子 → 截图 → 性能 → 网络 → 快捷方式 → 冗余清理 → 整合包 → AI → 音乐
T1 (17 tab):  日志 → 存档 → 种子 → 截图 → 性能 → 网络 → 快捷方式 → 冗余清理 → 整合包 →
              备份管理器 → NBT编辑器 → 数据包冲突检测 → 服务器资源包缓存 → 文件变更检测 → 年度报告 →
              AI → 音乐
```

### 设计决策记录

1. **文件变更检测首次运行只建基线不报"全新增"**：避免用户打开启动器时被几百个 Mod 的 Toast 淹没。
2. **面板预览用 `PreviewChanges`，启动通知用 `DetectAndUpdate`**：前者反复看不改基线，后者更新基线后再通知不再重复提醒。
3. **NBT 树结构变化后整树重建**：NBT 文件通常不大，比增量同步更稳。
4. **数据包冲突跳转用 `explorer.exe /select`**：在资源管理器中定位包文件，比文件管理器内建浏览更符合用户习惯。
5. **年度报告 Token 用 Deflate + Base64Url**：完全离线，复制粘贴即可分享，不需要联网或同一份存档。
6. **服务器资源包缓存面板不修改进服的缓存逻辑**：仅可视化现有索引，LRU 淘汰策略由 Core 在进服时自动执行。
7. **ToolboxView 仍为 TabControl 布局**：侧边栏 + 路由改造属于第二梯队，第一梯队仅补面板数量，后续拆 TabItem 到侧边栏即可复用已写的所有 View。

### 各梯队剩余计划

1. **第二梯队** — 规格符合度整改：全局换 ToggleSwitch、工具箱改侧边栏并打通路由、侧边栏点击保持展开、设置页补 7 个开关。
2. **第三梯队** — 补齐半成品：推荐按钮绑定、服务器添加/右键、统计真实数据、性能曲线、存档四操作、AI 助手改版。
3. **第四梯队** — 大工程新开发：皮肤编辑器像素引擎、开发工具三件套、HUD 叠加窗口、挂机工作流编辑器、真实连接服务器。
