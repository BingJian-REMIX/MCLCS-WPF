# 备份快照与历史归档不含一手源码，排除语言统计，避免仓库被误识别为 HTML
dist/** linguist-vendored
history/** linguist-vendored

# 让 .sln / .csproj 等正确归类
*.cs linguist-language=C#

# 发布产物（大文件走 LFS，避免原始 blob 直接入库）
tools/dist/** filter=lfs diff=lfs merge=lfs -text
